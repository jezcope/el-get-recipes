(:name mmm-mode
       :description "Allow Multiple Major Modes in a buffer"
       :type github
       :pkgname "purcell/mmm-mode"
       :build `(("./autogen.sh")
                ,(format "EMACS=%s ./configure" el-get-emacs))
                ("make"))
       :info "mmm.info")
